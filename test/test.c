#include<stdio.h>

int main()
{
    printf("hi pwq, you back finally.\n");

    int BytesOfint = 0;
    printf("In my compute, there is %d bytes composing int.\n", sizeof(BytesOfint));
    long int BytesOfLongint = 0;
    printf("In my compute, there is %d bytes composing long int.\n", sizeof(BytesOfLongint));
    return 0;
}